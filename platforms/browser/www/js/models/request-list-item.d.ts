export declare class RequestListItem {
    id: number;
    requestDate: string;
    name: string;
    school: string;
    service: string;
    status: string;
    action: string;
    constructor(id: number, requestDate: string, name: string, school: string, service: string, status: string, action: string);
}
